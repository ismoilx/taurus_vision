# Grafana — Dashboard provider auto-provisioning
apiVersion: 1

providers:
  - name: "Taurus Vision"
    orgId: 1
    folder: "Taurus Vision"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards